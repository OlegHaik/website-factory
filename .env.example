# Supabase Configuration
# Get these values from your Supabase project settings
# Go to: Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Default domain (optional)
# Used for localhost/vercel preview where the Host header isn't a real custom domain.
NEXT_PUBLIC_DOMAIN=

# Local/preview override (optional)
# If NEXT_PUBLIC_DOMAIN isn't set and you're on localhost, you can force a specific site
# by its slug (must exist in the `sites` table).
NEXT_PUBLIC_SITE_SLUG=
